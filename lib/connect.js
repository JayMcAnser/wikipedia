/**
 * connection the wiki interface to the main interface
 */


let Settings = {
  log: function(level, message, location) {
    console.log(level, message, location)
  }
}

module.exports = Settings;
